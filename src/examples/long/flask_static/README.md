# Static files and Flask

This example explores flask and the static folder

Conclusions:
* Flask does not automatically serve `index.html` if no route is given.
* Flask's default static folder is `static`.
