1. play around with idle. Use commands like:
	print "foo"
	x=7
	print x
	x=x/2
	print x
	etc...
2. run an editor (preferably something nice like notepad++).
	Write some python code in it.
	Save it as [somefile].py
	run it from the command line using
	C:\blabla\> python [somefile].py
